---
description: 板書版型已鎖定；禁止擅自修改 render.js / board.css 排版邏輯
globs: js/render.js,css/board.css
alwaysApply: false
---

# 板書版型（已固定，勿改）

使用者已確認現有板書「整齊、乾淨俐落」。**除非使用者明確要求修改板書排版，否則不得變更** `js/render.js` 與 `css/board.css` 中與板書顯示相關的邏輯與樣式。

## 固定規格摘要

### 選項 (A)～(E)
- `(A)` 標籤獨立左欄；**續行併入同一 `chem-row`**，與第一行文字左緣對齊（懸掛縮排），不對齊選項字母。
- 標籤欄約 `2em`、欄間距約 `0.28em`；選項內行距約 `0.22em`。

### 數學式
- 分式用直式 `\dfrac`；算式續行（`≠ 1:1`、`mol`、句號）合併同一邏輯行。
- 算式行 `white-space: nowrap`，過長**僅左右滑**，不硬折行、不行內上下捲動。
- 一般 `math-block` 在板書行內為 `inline-block`；反應表 `math-block--array` 維持 block。

### 單位與字體
- `mol` 以 KaTeX `$\mathrm{mol}$` 渲染，與其他英文／數學符號字體一致。
- `$A:B = …$` 等比例式須正確渲染，不可露出裸 `$`。

### 數字註解
- 分式分子／分母、分壓等語意化註解邏輯維持現狀；禁止改回「代入數值」。

## 允許修改的情況

- 使用者**明確**提出板書排版新需求或 bug。
- 與板書無關的功能（API、prompts、風格庫匯入等）。

## 修改前

先說明會動到哪些行為；採**最小 diff**，不重構、不順手「優化」已滿意的版型。
